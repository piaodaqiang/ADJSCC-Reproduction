# Environment Notes

This folder records the software environment used for ADJSCC reproduction.

Before installing dependencies, check:

- Python version
- Conda environment name
- TensorFlow version
- CUDA and cuDNN compatibility
- GPU availability

Use `scripts/check_env.ps1` for a lightweight local check.
